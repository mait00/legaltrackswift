# 🔗 Настройка Cursor для работы с Xcode проектом

Cursor - это мощный редактор на базе VS Code, который отлично работает с Swift и Xcode проектами. Вот как настроить интеграцию:

## ✅ Что уже настроено

Я создал конфигурационные файлы:
- `.vscode/settings.json` - настройки редактора для Swift
- `.vscode/tasks.json` - задачи для сборки проекта
- `.vscode/launch.json` - конфигурация для отладки

## 🚀 Быстрая настройка

### 1. Откройте проект в Cursor

```bash
cd /Users/mait/legaltrackswift
cursor .
```

Или через Cursor:
- **File → Open Folder...**
- Выберите `/Users/mait/legaltrackswift`

### 2. Установите расширения (опционально)

Рекомендуемые расширения для Swift:
- **Swift** (by sswg) - поддержка Swift языка
- **CodeLLDB** - отладчик для Swift

Установка через Cursor:
1. Нажмите `⌘⇧X` (Extensions)
2. Найдите "Swift"
3. Установите расширение

### 3. Настройка Swift Language Server

Cursor автоматически использует SourceKit-LSP из Xcode. Убедитесь что:
- Xcode установлен
- Command Line Tools установлены: `xcode-select --install`

## 📝 Рабочий процесс

### Редактирование в Cursor

1. **Откройте Swift файлы в Cursor**
   - Все файлы в папке `LegalTrack/LegalTrack/` доступны
   - Cursor поддерживает автодополнение Swift
   - Подсветка синтаксиса работает автоматически

2. **Используйте Cursor для:**
   - ✏️ Редактирования кода
   - 🔍 Поиска по проекту
   - 📝 Рефакторинга
   - 🤖 AI-помощника (Cursor AI)

### Сборка и запуск в Xcode

1. **Откройте проект в Xcode:**
   ```bash
   open LegalTrack/LegalTrack.xcodeproj
   ```

2. **Или используйте задачи Cursor:**
   - `⌘⇧P` → "Tasks: Run Task"
   - Выберите "Build Xcode Project"

3. **Запуск:**
   - В Xcode: `⌘R` (Run)
   - Или через терминал: `xcodebuild -project LegalTrack/LegalTrack.xcodeproj -scheme LegalTrack -destination 'platform=iOS Simulator,name=iPhone 15 Pro'`

## 🎯 Рекомендуемый workflow

### Вариант 1: Cursor + Xcode (Рекомендуется)

1. **Редактирование:** Cursor
   - Быстрое редактирование кода
   - AI-помощник для написания кода
   - Поиск и навигация

2. **Сборка и запуск:** Xcode
   - Компиляция проекта
   - Запуск на симуляторе/устройстве
   - Отладка
   - Interface Builder (если используется)

### Вариант 2: Только Cursor

1. Редактирование в Cursor
2. Сборка через терминал: `⌘⇧P` → "Tasks: Run Task" → "Build Xcode Project"
3. Запуск через Xcode или `xcodebuild`

## 🔧 Настройки Cursor

### Горячие клавиши

- `⌘P` - Быстрый поиск файлов
- `⌘⇧F` - Поиск по проекту
- `⌘⇧P` - Команды
- `⌘B` - Сборка (через задачи)
- `⌘K ⌘S` - Настройки клавиатуры

### Полезные команды

- **Format Document**: `⇧⌥F`
- **Go to Definition**: `F12`
- **Find References**: `⇧F12`
- **Rename Symbol**: `F2`

## 📁 Структура проекта в Cursor

```
/Users/mait/legaltrackswift/
├── LegalTrack/              # Xcode проект
│   └── LegalTrack/          # Исходный код
│       ├── App/
│       ├── Core/
│       ├── Features/
│       └── Shared/
├── .vscode/                 # Настройки Cursor
│   ├── settings.json
│   ├── tasks.json
│   └── launch.json
└── ...
```

## 🐛 Отладка

### В Xcode (Рекомендуется)
- Установите breakpoints в Xcode
- Запустите проект (`⌘R`)
- Отладка работает как обычно

### В Cursor (Ограничено)
- Можно подключиться к запущенному процессу
- Используйте конфигурацию "Attach to Xcode" из launch.json

## ⚡ Советы

1. **Держите оба редактора открытыми:**
   - Cursor для редактирования
   - Xcode для сборки и запуска

2. **Используйте Cursor AI:**
   - `⌘K` - AI-редактирование
   - `⌘L` - AI-чат
   - Отлично работает с Swift кодом!

3. **Синхронизация:**
   - Файлы автоматически синхронизируются
   - Xcode автоматически подхватывает изменения из Cursor

4. **Git интеграция:**
   - Cursor имеет встроенный Git
   - Можно коммитить прямо из Cursor

## 🔍 Поиск и навигация

- `⌘P` - Быстрый поиск файлов
- `⌘⇧O` - Поиск символов в файле
- `⌘T` - Поиск символов в проекте
- `⌘⇧F` - Поиск по всему проекту

## ✅ Готово!

Теперь вы можете:
- ✅ Редактировать код в Cursor
- ✅ Использовать AI-помощника
- ✅ Собирать проект через задачи
- ✅ Запускать в Xcode

**Откройте проект:** `cursor /Users/mait/legaltrackswift`

---

**Примечание:** Для полной функциональности (Interface Builder, отладка, запуск) используйте Xcode. Cursor отлично подходит для редактирования кода и работы с AI.








